.venv/
android/vendor/
dist/
work/
**/dist/
**/work/

# Release signing material. NEVER commit these - the keystore is the only thing
# that lets you ship an update without every user losing their save.
android/signing/*.jks
android/signing/*.keystore
android/signing/*password*

# Frida Gadget binaries fetched by embed_cheats.py
android/vendor/gadget/
